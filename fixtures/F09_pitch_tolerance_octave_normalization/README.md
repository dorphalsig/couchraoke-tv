# F09 — Pitch tolerance + octave normalization (Normal/Golden only)

Purpose: verify:
- per-difficulty tolerance ranges (Section 6.3): Easy=2, Medium=1, Hard=0 semitones
- octave normalization (Section 6.4) prior to comparison

This fixture contains multiple small subcases as sibling folders.
Each subcase folder contains its own `song.txt`, `pitchFrames.jsonl`, and `expected.score.json`.
